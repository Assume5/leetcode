/**
 * @param {string} s
 * @param {string} t
 * @return {boolean}
 */
var isSubsequence = function(s, t) {
    let length=0;
    for(let i=0;i<t.length;i++){
        if(s[length]===t[i]){
            length+=1;
        }
    }
    return length===s.length;
};
