/**
 * @param {number[]} A
 * @return {number[]}
 */
var sortedSquares = function(A) {
    let result=[];
    for(let i=0;i<A.length;i++){
        result.push(A[i]**2);
    }
    return result.sort(function(a,b){return a-b;})
};
